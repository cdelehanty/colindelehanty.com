# Implementation Plans

Generated by the improve skill on 2026-08-19. Execute in the order below unless
dependencies say otherwise. Each executor must read the selected plan fully,
honor its STOP conditions, and update its row when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Restore supported Vercel builds on Node 22 | P1 | S | — | DONE |
| 002 | Add repeatable deployment verification | P1 | M | 001 | TODO |
| 003 | Align the public portfolio with Magnit/Apple wording | P1 | S | 001, 002 | IN PROGRESS — final preview verified; production deployment pending |
| 004 | Upgrade Astro and migrate content collections | P1 | L | 001, 002 | TODO |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) |
REJECTED (with one-line rationale)

## Dependency notes

- 002 follows 001 so every local and CI verification uses the same supported
  Node major as Vercel.
- 003 follows 002 so the first content-only production release is verified in a
  preview deployment before it reaches the custom domain.
- 004 follows 002 because the Astro v4-to-current migration touches content
  collection APIs, the build adapter, and the lockfile; it needs an independent
  verification gate.
- 003 and 004 must be separate pull requests/deployments. Do not mix Marcom copy
  edits with the framework migration.

## Findings considered and rejected

- Fluid Compute, cold-start prevention, and skew protection: the dashboard
  reports zero function invocations and the audited routes contain no
  request-time APIs. These features do not address the current deployment risk.
- On-demand concurrent builds and production build prioritization: unnecessary
  for the current low-frequency Hobby-plan deployment workflow.
- Preview deployment authentication: useful only if previews contain private
  material; it does not improve production uptime.
- Git LFS: current media is delivered from Cloudinary/Vimeo, so enabling it adds
  no deployment-safety benefit.
- Apple logo in the unused `src/features/info/` feature: no current page imports
  that feature. The live About-page logo is addressed in Plan 003.

## Operational follow-ups (do separately)

- Vercel reports that the existing legacy DNS records still work, but recommends
  changing the apex A record to `216.198.79.1` and the `www` CNAME to
  `1aa687e1311976ae.vercel-dns-017.com.` Make this registrar change only after a
  successful production deployment, and do not combine it with a code release.
- The Hobby plan can instantly roll back to the immediately previous production
  deployment. Record the known-good deployment URL in the runbook created by
  Plan 002.
